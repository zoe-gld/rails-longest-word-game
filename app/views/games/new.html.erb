<h1>New game</h1>

<ul class="list-inline">
  <% @letters.each do |letter| %>
    <li class="list-inline-item"> <%= letter %> </li>
  <% end %>
</ul>

<p>What is the longest word that you can find with these letters?</p>

<form class="form-inline" action="score" method="post">
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
  <%= hidden_field_tag :letters, @letters %>
  <input type="text" name="word" class="form-control" placeholder="Give it a try">
  <button type="submit" class="btn btn-primary">Submit</button>
</form>
